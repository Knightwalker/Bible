std::vector<data_type> name; // creates an empty vector

std::vector<data_type> name(elements, values) // creates a vector with (for example) 100 elements, each element's value is initialized to 5

std::vector<std::vector<data_type>> name; // creates an empty 2D vector

std::vector<std::vector<std::vector<data_type>>> name; // creates an empty 3D vector

std::vector<int> numbers {13, 42, 69} // declares a vector and initialize it directly (c++11)

std::vector<int> numbers = {13, 42, 69} // declares a vector and initialize it directly (c++11)