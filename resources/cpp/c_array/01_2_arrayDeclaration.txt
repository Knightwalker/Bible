// declare an array and initialize it with specific elements
int arr[] = { 10, 20, 30, 40 };  

// result
10 20 30 40

// explanation
by both declaring (without size) and initializing an array, by providing specific elements, the compiler creates an array with 4 elements, which are: 10, 20, 30, 40